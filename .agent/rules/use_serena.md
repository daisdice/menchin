# Serena Protocol
Always checks Serena's memories first.
When user asks for information or context, prioritizes using `serena` tools (e.g., `mcp_read_memory`, `mcp_find_symbol`) over reading raw files to save tokens.

